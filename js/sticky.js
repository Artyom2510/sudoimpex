$(function() {
	// //Липкая позиция
	// // var height = $(document).outerHeight(true);
	// var dotsFooter = $('.slider__dots-footer');
	// if ( $('.main__sliders').hasClass('translate') ) {
	// 	dotsFooter.addClass('fixed');
	// }
	// var sectSlidersHeight;
	// var sliders = $('.sliders');
	// function isSticky() {
	// 	sectSlidersHeight = sliders.outerHeight(true);
	// 	if( sectSlidersHeight > $(window).outerHeight(true) && $('body').hasClass('translate') ) {
	// 		dotsFooter.addClass('fixed');
	// 		var a = dotsFooter.offset().top;
	// 		var b = $(window).outerHeight(true) - dotsFooter.outerHeight(true) + $(window).scrollTop();
	// 		console.log(a, sectSlidersHeight, $(window).scrollTop(), b);
	// 		// dotsFooter.animate({top: b + 'px'}, 100);
	// 		dotsFooter.css('top', b + 'px');
	// 	}
	// }
	// isSticky();
	// $(window).on('resize', function() {
	// 	isSticky();
	// });
	// $(window).on('scroll', function () {
	// 	isSticky();
	// });
});
